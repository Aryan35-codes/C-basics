#include<stdio.h>
int main(){    // B , O , D/M(left to right in code) , A/S
    int m = 2 * 3 / 4 + 4 / 4 + 8 - 2 + 5 / 8;
    printf("%d\n",m);
   int i = 2, j = 3, k, l;
   float a,b;
  k = i / j * j;
   l = j / i * i;
   a = i / j * j;
   b = j / i * i;
   printf("%d\n %d\n %f\n %f\n", k, l, a, b);
  float x = 22.0/7;
  printf("%f",x);
    return 0;
}