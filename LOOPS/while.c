#include<stdio.h>               // while loop
int main(){
    int i = 0;
    while (i<101) // used when condition is not clear
    {             // sirf condition likhi hoti hain
        printf("%d ",i);
        ++i;
        
    }            
    
    
    return 0;
}