#include<stdio.h> // library fn=> jismein group of fn. hote hain
#include<math.h> // math ek library fn. hain
int main(){
    // int a;
    // printf("Enter a number:");
    // scanf("%d",&a);
    // int root = sqrt(a);
    // printf("The square root is:%d",root);
    int q = pow(2,5); //=> 2 to the power of 5
    printf("%d",q);

    return 0;
}