#include<stdio.h> // THE REAL THING //
int main(){
    int a;
    char ch = 'a';
    if(3 + 2 % 5) // if ke andar koi bhi integer value dedo woh true hota hain
        printf("This works\n");   // '0' false hota hain
    if(a=10)
    printf("Even this works\n");  
    if(-5)
    printf("Suprisingly even this works\n"); 
    if(ch = 'g')
    printf("HELLO");  
    return 0; 
}